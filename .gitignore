# build
dist/
.output/

# deps
node_modules/

# astro
.astro/

# cloudflare / wrangler — lokalny cache dev, stan KV/D1, tymczasowe bundle
.wrangler/

# sekrety lokalne wranglera (np. BASIC_AUTH_PASS dla `wrangler pages dev`)
.dev.vars
.dev.vars.*

# klucz prywatny GitHub App (bot deployu) — NIGDY nie commitować
*.pem

# env
.env
.env.production
.env.*.local

# robocze, nie do publikacji — zostają lokalnie (research notes, makiety social,
# surowe materiały źródłowe: screenshoty, zrzuty, bookmarki)
research/
social/
/dane/
petycja_screenshots/

# skrypty operacyjne (deploy przez bota, GitHub App) — lokalne, nie do publikacji
scripts/

# narzędzia / agent (config lokalny, nie do publikacji)
.claude/
CLAUDE.md

# system
.DS_Store
*.log
npm-debug.log*
