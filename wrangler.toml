name = "zarobkilekarzy"
pages_build_output_dir = "dist"
compatibility_date = "2025-06-01"

# Sonda (wyniki): w panelu Cloudflare Pages → Settings → Bindings dodaj
# KV namespace binding o nazwie SONDA_KV. Bez niego /api/sonda zwraca 503,
# a sonda działa w trybie bez wyników zbiorczych (graceful fallback).
